{
  "name": "tubedigest",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "prepare": "husky"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "husky": "^9.0.0"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
